import { RolePlaceholder } from '@/components/RolePlaceholder';

export default function ManualEntry() {
  return <RolePlaceholder title="Manual PIN" body="Fallback confirmation for busy rooms." />;
}
