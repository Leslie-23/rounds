import { RolePlaceholder } from '@/components/RolePlaceholder';

export default function VenuesQueue() {
  return <RolePlaceholder title="Venue queue" body="Approve, reject, or pause participating venues." />;
}
